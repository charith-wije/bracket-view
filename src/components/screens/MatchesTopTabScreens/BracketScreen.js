import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const BracketScreen = () => {
  return (
    <View style={{backgroundColor: '#011f8c', flex: 1}}>
      <Text>BracketScreen</Text>
    </View>
  );
};

export default BracketScreen;

const styles = StyleSheet.create({});
